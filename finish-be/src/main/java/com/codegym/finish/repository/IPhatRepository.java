package com.codegym.finish.repository;

import com.codegym.finish.model.Phat;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IPhatRepository extends JpaRepository<Phat,Integer> {
}
